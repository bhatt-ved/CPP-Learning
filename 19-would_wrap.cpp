/*
 * This program will take a filename as an argument and tell if the file contains
 * any line that would wrap (not fit) on an old UNIX terminal.  (UNIX terminals
 * have width of 80 characters.
 *
 */

